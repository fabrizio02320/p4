
    <div class="form-group">
        {{ form_label(ticket.tarifReduit) }}
        {{ form_errors(ticket.tarifReduit) }}
        {{ form_widget(ticket.tarifReduit, {'attr':
            {'onclick':
                'if(this.checked){this.parentElement.parentElement.nextElementSibling.setAttribute("style", "display: block")}
                else{this.parentElement.parentElement.nextElementSibling.setAttribute("style", "display: none")}'
            }
        }) }}
        <p class="info-tarif-reduit" style="display: none">
            <i>Il sera nécessaire de présenter votre carte d’étudiant, militaire ou équivalent lors de votre entrée dans le musée.</i>
        </p>
    </div>
    <div class="form-group">
        {{ form_label(ticket.nom, 'NOM') }}
        {{ form_errors(ticket.nom) }}
        {{ form_widget(ticket.nom, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_label(ticket.prenom, 'Prénom') }}
        {{ form_errors(ticket.prenom) }}
        {{ form_widget(ticket.prenom, {'attr': {'class': 'form-control'}}) }}
    </div>
    {#<div class="form-group">#}
        {#{{ form_label(ticket.ddn, 'Date de naissance') }}#}
        {#{{ form_errors(ticket.ddn) }}#}
        {#{{ form_widget(ticket.ddn) }}#}
    {#</div>#}
    <div class="form-group">
        {{ form_label(ticket.ddn, 'Date de naissance') }}
        {{ form_errors(ticket.ddn) }}
        <br />
        {#{{ form_widget(ticket.ddn.day) }}#}
        {#{{ form_widget(ticket.ddn.month) }}#}
        {#{{ form_widget(ticket.ddn.year) }}#}
        {#<div class="ddn-form">#}
            {#{{ form_widget(ticket.ddn.day, {'attr': {'style': 'width:33%'}}) }}#}
            {#{{ form_widget(ticket.ddn.month, {'attr': {'style': 'width:33%'}}) }}#}
            {#{{ form_widget(ticket.ddn.year, {'attr': {'style': 'width:33%'}}) }}#}
        {#</div>#}
        <div class="ddn-form">
            {{ form_widget(ticket.ddn.day, {'attr': {'style': 'width:125%'}}) }}
            {{ form_widget(ticket.ddn.month, {'attr': {'style': 'width:125%'}}) }}
            {{ form_widget(ticket.ddn.year, {'attr': {'style': 'width:175%'}}) }}
        </div>
    </div>
    <div class="form-group">
        {{ form_label(ticket.pays, 'Pays') }}
        {{ form_errors(ticket.pays) }}
        {{ form_widget(ticket.pays, {'attr': {'class': 'form-control'}}) }}
    </div>